# set(name_of_list) makes list items unique
# len() : length
# range() : range of integers
# sorted() : sorts a list
# enumerate() : returns list of tuples from the list [(0, value), (1, value), (2, value)]
# zip() : returns list of tuples with element from each iterable
## useful for looping through multiple lists
## for elem1, elem2 in zip(a, b):
##     print 'eleme1 is {0} and elem2 is {1}'.format(elem1, elem2)

# easier string formatting
## print name, 'is in the', index, 'position' <-- adds spaces where commas are
## message = '{0} is in the {1} position'.format(name, index)